{
  "name": "monorepo-example",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:react": "turbo run start --filter=react-app",
    "build:react": "turbo run build --filter=react-app",
    "dev:umi": "turbo run start --filter=umi-app",
    "build:umi": "turbo run build --filter=umi-app",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@tsconfig/create-react-app": "1.0.3",
    "prettier": "latest",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {},
  "packageManager": "yarn@1.22.4"
}